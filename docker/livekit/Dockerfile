# LiveKit Server - use official image
FROM livekit/livekit-server:latest

# Expose port
EXPOSE 7880

# Run in development mode with env vars
CMD ["livekit-server", "--dev", "--listen-address", ":7880"]